import 'package:flutter/material.dart';

class NewActivityPage extends StatelessWidget {
  const NewActivityPage({super.key, required int activityType});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
